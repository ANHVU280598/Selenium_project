summary: Get all SOP names
description: Returns a list of SOP names available in the system.
responses:
  200:
    description: A list of SOP names
    schema:
      type: array
      items:
        type: string
    examples:
      application/json: ["SA6118", "SA6223", "SA7069"]